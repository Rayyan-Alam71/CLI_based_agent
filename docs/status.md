Step 5 => 
    We are gonna implement task based persistence, which we can use to return to a session.
    Todos are for short todo checklist

# What to achieve
    1. LLM generates list of tasks
    2. It saves them into the .task directory
    3. It loads current task to do, and mark it completed | failed | progress
    4. When the user exits, the .tasks dir should uphold the status of the workflow

